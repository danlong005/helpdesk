import React from "react";
import CreateForm from "./CreateForm";

const CreateTicket = () => {
  return (
    <main>
      <h2 className="text-primary">Create New Ticket</h2>
      <CreateForm />
    </main>
  );
};

export default CreateTicket;
